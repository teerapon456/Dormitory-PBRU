# ระบบจัดการหอพัก (Dormitory Management System)

ระบบจัดการหอพักแบบครบวงจร พัฒนาด้วย PHP และ MySQL สำหรับจัดการข้อมูลหอพัก นักศึกษา การแจ้งซ่อม และค่าใช้จ่ายต่างๆ

## คุณสมบัติหลัก

- 🏠 จัดการข้อมูลอาคารและห้องพัก
- 👥 จัดการข้อมูลผู้ใช้ (ผู้ดูแลระบบ, นักศึกษา)
- 🔧 ระบบแจ้งซ่อม
- 💰 จัดการค่าใช้จ่ายและบิล
- 📊 รายงานและสถิติต่างๆ
- 🔐 ระบบยืนยันตัวตนและการจัดการสิทธิ์

## การติดตั้ง

### ความต้องการของระบบ

- PHP 7.4 ขึ้นไป
- MySQL 5.7 ขึ้นไป
- Web Server (Apache/Nginx)
- Composer
- Node.js และ npm

### ขั้นตอนการติดตั้ง

1. Clone หรือดาวน์โหลดโปรเจค

```bash
git clone <repository-url>
cd dormitory
```

2. ติดตั้ง Dependencies ผ่าน Composer

```bash
composer install
```

3. ติดตั้ง Node Modules

```bash
npm install
```

4. สร้างฐานข้อมูล
- สร้างฐานข้อมูลใหม่ใน MySQL
- นำเข้าไฟล์ SQL จากโฟลเดอร์ database:
  - `dormitory_db.sql` (โครงสร้างฐานข้อมูล)
  - `sample_data.sql` (ข้อมูลตัวอย่าง)

5. ตั้งค่าการเชื่อมต่อฐานข้อมูล
- คัดลอกไฟล์ `config/config.example.php` เป็น `config/config.php`
- แก้ไขค่าการเชื่อมต่อฐานข้อมูลให้ถูกต้อง

6. ตั้งค่าสิทธิ์การเข้าถึงไฟล์

```bash
chmod -R 755 .
chmod -R 777 uploads
```

## โครงสร้างโฟลเดอร์

```
dormitory/
├── assets/              # ไฟล์ CSS, JavaScript, รูปภาพ
├── config/              # ไฟล์การตั้งค่าระบบ
├── database/            # ไฟล์ SQL
├── includes/            # ไฟล์ PHP ที่ใช้ร่วมกัน
├── logs/               # Log files
├── modules/            # โมดูลต่างๆ ของระบบ
│   ├── admin/         # ส่วนผู้ดูแลระบบ
│   ├── users/         # ส่วนผู้ใช้งาน
│   └── public/        # ส่วนสาธารณะ
├── node_modules/       # Node.js packages
├── uploads/           # ไฟล์ที่อัพโหลด
├── vendor/            # Composer packages
└── ...
```

## การใช้งานระบบ

### ส่วนผู้ดูแลระบบ
- URL: `/modules/admin/`
- Username: admin
- Password: admin123

### ส่วนนักศึกษา
- URL: `/modules/users/`
- สามารถลงทะเบียนผ่านหน้า register.php

### ส่วนสาธารณะ
- URL: `/modules/public/`
- สามารถแจ้งซ่อมโดยไม่ต้องล็อกอิน

## ฟีเจอร์หลัก

### ระบบจัดการผู้ใช้
- จัดการข้อมูลผู้ใช้
- กำหนดสิทธิ์การเข้าถึง
- จัดการข้อมูลส่วนตัว

### ระบบจัดการห้องพัก
- จัดการข้อมูลอาคาร
- จัดการข้อมูลห้องพัก
- ติดตามสถานะห้องพัก

### ระบบแจ้งซ่อม
- แจ้งซ่อมออนไลน์
- ติดตามสถานะการซ่อม
- จัดการคำขอแจ้งซ่อม
- อัพโหลดรูปภาพประกอบ

### ระบบจัดการค่าใช้จ่าย
- จัดการค่าเช่า
- จัดการค่าสาธารณูปโภค
- ออกใบแจ้งหนี้
- ติดตามการชำระเงิน

### ระบบรายงาน
- รายงานการเข้าพัก
- รายงานการแจ้งซ่อม
- รายงานการเงิน
- สถิติต่างๆ

## การพัฒนาเพิ่มเติม

1. Fork โปรเจค
2. สร้าง Feature Branch (`git checkout -b feature/amazing-feature`)
3. Commit การเปลี่ยนแปลง (`git commit -m 'Add some amazing feature'`)
4. Push ไปยัง Branch (`git push origin feature/amazing-feature`)
5. เปิด Pull Request

## เทคโนโลยีที่ใช้

- PHP 7.4
- MySQL 5.7
- Bootstrap 5
- Font Awesome 5
- jQuery
- TCPDF (สำหรับสร้าง PDF)
- Sarabun Font (Google Fonts)

## ผู้พัฒนา

- [ชื่อผู้พัฒนา] - [อีเมล]

## License

โปรเจคนี้อยู่ภายใต้ [MIT License](LICENSE) 